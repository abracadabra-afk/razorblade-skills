---
type: dev-anchor
project: {{PROJECT}}
profile: {{PROFILE}}
file: dev-anchor
last_updated: {{DATE}}
---

# {{PROJECT}} — Taste Anchor

> **What this is:** the project-level steering signal — *what I love about this book and what I know so far.* Taste is a first-class field, not exhaust. Every scene in `scenes/` is measured against this; the anchor propagates **down** the spine (anchor → sequence read → scene), while authority on *what happens* flows **up** (scene → sequence → `project.md`).
> **How it grows:** `dev-capture` appends/sharpens this when CRE cues "project level…" / "what I love about this book…". Sculptor, not historian — sharpen in place; the raw transcript is the floor in `_intake/_audit/`.

## What I love about this
<!-- the molten core: the images, feelings, questions that make this book worth writing. Captured verbatim-in-spirit, never tidied into blandness. -->
_(empty — fill by cueing "what I love about this book…")_

## What I know so far
<!-- the settled-enough macro reads: tone targets, the central question, the felt shape. Soft, revisable, never canon until promoted. -->
_(empty)_

## Tone targets
<!-- the emotional register the prose is reaching toward — the measuring stick for drift. -->
_(empty)_

## Open questions I'm holding
<!-- ambiguities CRE has deliberately NOT resolved. PRESERVE THE KIND — a question stays a question. -->
_(empty)_

---
*Downward-propagation note: when this anchor sharpens, `dev-capture` flags any `scenes/` entry whose `Taste` drifts from it — it never rewrites the scene, only surfaces the drift for CRE.*
