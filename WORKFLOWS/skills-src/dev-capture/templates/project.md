---
type: dev-project
project: {{PROJECT}}
profile: {{PROFILE}}
file: macro-read
last_updated: {{DATE}}
---

# {{PROJECT}} — Macro Read

> **The macro read — DERIVED, authority flows up.** Logline, what it's about, tone targets. This is a running synthesis *of the sequences below it*, which are themselves derived from scenes. `dev-capture` may note here that the project read *shifted* when a scene changed — silently, no ceremony. Never hand-author canon here; this mirrors the granular layer.
> (Absent in the short-story profile — at that size the taste anchor `_DEV.md` *is* the macro read.)

## Logline
_(empty)_

## What it's about
_(empty)_

## Tone targets
_(mirrors / refines `_DEV.md`)_

## Shape (sequences in order)
<!-- a derived running list as sequences accrue; each links to its sequences/ entry -->
_(empty)_
